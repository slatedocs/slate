ingress:
  enabled: true
  hosts:
    - host: documentation.tier-ops.com
      paths:
        - /
  tls:
    - secretName: documentation-tls-secret
      hosts:
      - documentation.tier-ops.com

resources:
  limits:
    memory: 512m
  requests:
    memory: 512m

env:
  variables:
    data: null
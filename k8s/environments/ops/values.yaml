ingress:
  enabled: true
  hosts:
    - host: documentation.tier-ops.com
      paths:
        - /
  tls:
    - secretName: documentation-tls-secret
      hosts:
      - documentation.tier-ops.com

resources:
  limits:
    memory: 512Mi
  requests:
    memory: 512Mi

env:
  variables:
    data: null

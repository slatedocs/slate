ingress:
  enabled: true
  hosts:
    - host: documentation.tier-staging.io
      paths:
        - /
  tls:
    - secretName: documentation-tls-secret
      hosts:
      - documentation.tier-staging.io

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 1000m
    memory: 1Gi

env:
  variables:
    data: null